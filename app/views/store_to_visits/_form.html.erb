<%= form_for(@storeToVisit) do |f| %>
  <% if @storeToVisit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@storeToVisit.errors.count, "error") %> prohibited this store from being saved:</h2>

      <ul>
      <% @storeToVisit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div align="center" class="field">
  <%= f.label :store_id %>
	<br>
	<%= f.select :store_id, @store_options %>
</div>
<%= f.hidden_field :user_id, value: current_user.id %>

<div class="modal-footer actions">
	<button type="button" class="btn btn-default" data-dismiss="modal">
		Close
	</button>
	<%= f.submit class: "btn btn-primary"%>
</div>
<% end %>
